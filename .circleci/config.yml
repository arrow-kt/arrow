version: 2

jobs:
  build:
    working_directory: ~/arrow
    docker:
      - image: circleci/openjdk:8-jdk-browsers
    environment:
      GRADLE_OPTS: '-Dkotlin.compiler.execution.strategy=“in-process”'
    steps:
      - checkout
      - run:
          name: Clean build
          command: ./gradlew --stacktrace clean build
